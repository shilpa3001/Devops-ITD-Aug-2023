apiVersion: v1
kind: Pod
metadata:
  name: ip-app-resource-quota
spec:
  containers:
  - name: ip-app
    image: harshajain/ip_app
    ports:
    - containerPort: 80
    resource:
      requests:
        memory: 100Mi
      limits:
        memory: 150Mi
  priorityClassName: high-priority
